services:
  api:
    build: .
    container_name: api_cicd_ejercicio
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload